# P08 qualification record

## Version 1.1.0 extension qualification

Skald 1.1.0/build 2 adds Markdown-plus-JSON conversion in one run and optional delivery of the unchanged original with generated outputs in one folder. The current matrix contains 118 native tests and 17 required sentinel identifiers. Final Debug, Release, AddressSanitizer, ThreadSanitizer, and Xcode Product Test runs each passed 118/118 with zero failures or skips. Static analysis and all three supplied validators passed; current hosted Debug and Release runs match those counts.

Xcode Product Build succeeded and Product Run exposed selectable Markdown, JSON, Both, and Bundle original with outputs controls. The separately built signed Release app passed strict signature verification, retained the application sandbox and user-selected read/write entitlement, and reported version 1.1.0/build 2. Its Both-plus-bundle conversion delivered exactly `article.txt`, `article.md`, and `article.json`; the source and bundled original had identical SHA-256 values and the JSON parsed successfully.

The final external ledger maps all 12 gates and all 76 required package entries to current source-bound evidence and passes `check_ledger.py`. The historical 1.0.0 qualification below remains evidence for direct Finder drag and the pre-extension manual format matrix. The current native rerun covers the unchanged intake and format paths, while the signed 1.1.0 app directly exercises the changed output controls and publication path. This remains a qualification of documented scope, without an App Store submission or public release claim.

## Version 1.0.0 historical qualification

Skald remains at application version **1.0.0**, Xcode build **1**. The CSV/TSV, PDF, image, XML, workbook, and ZIP output schema **2.0** is separate. The completed remediation supports the named format variants and limits in `NAMED_FORMAT_READERS.md`; this record is a qualification of that documented scope, not a claim that every historical spreadsheet, archive, or attributed-document variant is supported.

The final local automated collector on macOS 26.6.2 arm64 with Xcode 26.6, Swift 6.3.3, and SDK 26.5 completed signed Debug and Release builds, **107 passed, 0 failed, 0 skipped** in each configuration, static analysis, and the conversion, digital-PDF, and output-planner fixture validators. Separate AddressSanitizer and ThreadSanitizer test runs each passed **107/107**, with no failed or skipped tests. Xcode's actual Product → Build succeeded, Product → Test passed **107/107**, and Product → Run launched the signed app. The package collector's before/after source fingerprints matched. Raw xcresults, logs, environment output, and hashed per-gate records are kept outside the checkout in the P08 evidence directory.

The standalone signed Release app passed strict code-signature verification. Its effective entitlements contained `com.apple.security.app-sandbox`, selected-file read/write access, and development `get-task-allow`; there was no network or temporary file/Mach-lookup exception in that standalone build. The signed Debug IDE console and the default signed Release log did not expose synthetic source paths or contents after the logging repair. The Xcode test-host build had additional runner-only temporary exceptions, so production entitlement conclusions use the separate standalone Release build.

Signed app checks exercised an individual CSV, Windows-1252 override, LF/CRLF/UTF-16 and malformed CSV cases, a multi-file native picker, hidden and nested intake, both output formats, an occupied target, equal source/target and nested target, scanned/mixed PDF, multipage TIFF, ordered XML, resource-bearing HTML denial, and the XLSX/BIFF8 XLS/ODS/ZIP collection. A final mixed-folder Product Run reported **6 converted, 4 partial, 1 skipped, 1 failed of 12 planned**; ten parseable JSON outputs were published, while unsafe HTML and unknown binary input produced no normal output. A signed Release 6,000-file batch showed active progress, cancelled before all inputs, restarted immediately, and left no owned temporary files. Native writer tests and source-hash inspections established no source or pre-existing target replacement in the exercised cases.

The separate Xcode `SkaldInteraction` UI-test scheme performed a direct two-file Finder-to-Skald drag into the Sources row. Finder selected `a.csv` and `b.txt`; Skald reported **2 selected** afterward, and the recorded interaction test passed with no failures. A two-file native picker independently converted both files, and the compiled production ViewModel drop-path test passed. Xcode printed negative-view-geometry diagnostics at launch and after conversion reports. The signed window remained responsive, the controls and report were visible with vertical scrolling, and the previous state-publication warning disappeared in fresh picker, output-selection, and batch runs. Those geometry diagnostics remain an observed rendering issue rather than a clean-console claim.

The four package review prompts were executed as direct behavior, oracle-integrity, safety/concurrency, and evidence reviews in `P08_REVIEW.md`. They found and repaired a BIFF8 worksheet-boundary/EOF bug, quadratic XML text concatenation, source-path console disclosure, and SwiftUI state-update warnings. A real negative CFB/BIFF8 workbook fixture, source-bound native runs, and signed IDE checks verify the changes. Hosted macOS CI is configured with a verified `macos-26`/Xcode 26.6 pairing, full action commit pins, retained results, and a 107-test/required-identifier/no-skip guard. The [source-fix run](https://github.com/flynn33/Skald/actions/runs/35001841134) completed successfully at commit `4a71ef1c010470f5d2747a41d496a6d7592b42c2`; its downloaded Debug/Release xcresults each report 107 passed, 0 failed, 0 skipped. The final documentation-commit run and artifacts are recorded in the separate gate evidence. No App Store submission, public distribution, or universal format coverage is asserted by this qualification record.
