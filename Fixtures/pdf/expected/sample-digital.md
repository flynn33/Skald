# Sample Digital

## Page 1

### Quarterly Overview

Skald converts documents into readable Markdown and JSON so teams can review content
without wrestling with raw text streams or broken layouts. This sentence is intentionally
long so that it wraps across several lines and helps push the document onto a second page
to exercise multi-page extraction and the running header and footer detection logic.

##### Key Points

- First bullet item describing an important detail worth noting.
- Second bullet item with more information that should also wrap cleanly across lines.
- Third bullet item.

##### Background

Paragraph number 1: additional narrative content to fill the page and ensure the document
spans at least two pages so the running header and footer appear on every page and can be
detected as repeated common lines by the parser.

Paragraph number 2: additional narrative content to fill the page and ensure the document
spans at least two pages so the running header and footer appear on every page and can be
detected as repeated common lines by the parser.

Paragraph number 3: additional narrative content to fill the page and ensure the document
spans at least two pages so the running header and footer appear on every page and can be
detected as repeated common lines by the parser.

Paragraph number 4: additional narrative content to fill the page and ensure the document
spans at least two pages so the running header and footer appear on every page and can be
detected as repeated common lines by the parser.

Paragraph number 5: additional narrative content to fill the page and ensure the document
spans at least two pages so the running header and footer appear on every page and can be
detected as repeated common lines by the parser.

Paragraph number 6: additional narrative content to fill the page and ensure the document
spans at least two pages so the running header and footer appear on every page and can be
detected as repeated common lines by the parser.

## Page 2

Paragraph number 7: additional narrative content to fill the page and ensure the document
spans at least two pages so the running header and footer appear on every page and can be
detected as repeated common lines by the parser.

Paragraph number 8: additional narrative content to fill the page and ensure the document
spans at least two pages so the running header and footer appear on every page and can be
detected as repeated common lines by the parser.

Paragraph number 9: additional narrative content to fill the page and ensure the document
spans at least two pages so the running header and footer appear on every page and can be
detected as repeated common lines by the parser.

Paragraph number 10: additional narrative content to fill the page and ensure the document
spans at least two pages so the running header and footer appear on every page and can be
detected as repeated common lines by the parser.
